name: Check connection

on:
  workflow_dispatch:
    inputs:
      url:
        description: "URL to test"
        required: true
        type: string

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Check connection
        run: |
          curl -I --connect-timeout 15 ${{ github.event.inputs.url }} || exit


