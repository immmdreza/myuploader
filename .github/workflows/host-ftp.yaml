name: Download and Upload via FTP

on:
  workflow_dispatch:
    inputs:
      file_url:
        description: "URL of the file to download"
        required: true
        type: string

jobs:
  upload-via-ftp:
    runs-on: ubuntu-latest

    steps:
      - name: Download file
        run: |
          cd /tmp
          curl -L -O "${{ github.event.inputs.file_url }}"
          echo "Downloaded files:"
          ls -la /tmp

      - name: Upload via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: 3002048091.cloudylink.com
          username: webapp@cutify.ir
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: /tmp/

